---
title: "Configura MCP para Cursor, Claude, Windsurf y más"
sidebarTitle: "MCP setup"
description: "Conecta Cursor, Claude, Windsurf y otros editores a Yarah mediante MCP para que los agentes de IA operen tu base de datos, autenticación y almacenamiento."
---

## Resumen

El servidor MCP de Yarah le da a tu asistente de codificación con IA acceso directo a tu backend: consultas de base de datos, gestión de esquemas, almacenamiento y más.

Sigue las instrucciones a continuación para tu cliente de IA. ¿No ves tu herramienta? Consulta el [directorio de agentes](https://yarah.dev/agents) para ver la lista completa de integraciones probadas.

## Requisitos previos

- Un **asistente de codificación con IA** (Cursor, Claude Code, GitHub Copilot, etc.)
- Un **proyecto de Yarah** - [crea uno aquí](https://yarah.dev) si aún no tienes uno

## Configuración local de MCP (recomendado)

Si prefieres configurar MCP localmente, selecciona tu asistente de codificación con IA a continuación:

<AccordionGroup>
  <Accordion title="Cursor">
    ### Instalación

    Puedes instalar el servidor MCP en Cursor con un solo clic, haciendo clic en el botón "Add to Cursor" de la captura de pantalla a continuación:

    ![Connect Project](/images/mcp-setup/connect-project.png)

    Alternativamente, puedes instalar el servidor MCP de Yarah manualmente:

    1. Abre **Cursor Settings**
    2. Ve a **Tools & MCP**
    3. Haz clic en **New MCP Server (Add a Custom MCP Server)**

    <Frame>
      <video
        src="/images/mcp-setup/Cursor-MCP-1.mp4"
        autoPlay
        muted
        loop
        playsInline
      />
    </Frame>

    4. Pega el JSON de MCP de tu proyecto de Yarah
    5. Guarda el archivo de configuración
    6. Deberías ver un punto verde, que indica que Yarah se instaló correctamente

    <Frame>
      <video
        src="/images/mcp-setup/Cursor-MCP-2.mp4"
        autoPlay
        muted
        loop
        playsInline
      />
    </Frame>

    ### Verificar la instalación

    Para verificar la conexión, inicia una nueva sesión de chat en **Cursor** y envía este mensaje a tu agente:

    ```
    I'm using Yarah as my backend platform, call Yarah MCP's fetch-docs tool to
    learn about Yarah instructions.
    ```

    Si la conexión se realiza correctamente, verás el indicador de estado **Connected** en tu panel de Yarah (esquina superior derecha).

    <video
      controls
      autoPlay
      muted
      loop
      playsInline
      width="100%"
      src="/images/mcp-setup/Cursor-MCP-3.mp4"
    />
  </Accordion>

  <Accordion title="Claude Code">
    ### Instalación

    Agrega el servidor MCP de Yarah a tu **Claude Code**:

    1. Selecciona **Claude Code** en la lista desplegable
    2. Copia y pega el siguiente comando de instalación en tu terminal

    ![Claude Code Connect](/images/mcp-setup/claude-code-connect.png)

    3. Ejecuta el comando

    <Frame>
      <video
        src="/images/mcp-setup/CC-MCP-1.mp4"
        autoPlay
        muted
        loop
        playsInline
      />
    </Frame>

    ### Verificar la instalación

    Para verificar la conexión, inicia una nueva sesión de chat en **Claude Code** y envía este mensaje a tu agente:

    ```
    I'm using Yarah as my backend platform, call Yarah MCP's fetch-docs tool to
    learn about Yarah instructions.
    ```

    Si la conexión se realiza correctamente, verás el indicador de estado **MCP Connected** en tu panel de Yarah (esquina superior derecha).

    <video
      controls
      autoPlay
      muted
      loop
      playsInline
      width="100%"
      src="/images/mcp-setup/CC-MCP-2.mp4"
    />
  </Accordion>

  <Accordion title="GitHub Copilot">
    ### Instalación

    Agrega el servidor MCP de Yarah a tu **GitHub Copilot**:

    1. Selecciona **Copilot** en la lista desplegable
    2. Abre la terminal y pega el siguiente comando de instalación en tu terminal de **GitHub Copilot**

    ![Copilot Setup](/images/mcp-setup/copilot-1.png)

    3. Ejecuta el comando

    ![Copilot Install](/images/mcp-setup/copilot-2.png)

    ### Verificar la instalación

    Para verificar la conexión, inicia una nueva sesión de chat en **Copilot** y envía este mensaje a tu agente:

    ```
    I'm using Yarah as my backend platform, call Yarah MCP's fetch-docs tool to
    learn about Yarah instructions.
    ```

    Si la conexión se realiza correctamente, verás el indicador de estado **MCP Connected** en tu panel de Yarah (esquina superior derecha).

    ![MCP Connected](/images/mcp-setup/mcp-connected.png)
  </Accordion>

  <Accordion title="Google Antigravity">
    ### Instalación

    Agrega el servidor MCP de Yarah a tu **Antigravity**:

    1. Selecciona **Antigravity** en la lista desplegable
    2. Copia y pega el siguiente comando de instalación en tu terminal de **Antigravity**

    ![Antigravity Setup](/images/mcp-setup/antigravity-1.png)

    3. Ejecuta el comando

    <Frame>
      <video
        src="/images/mcp-setup/Antigravity-MCP.mp4"
        autoPlay
        muted
        loop
        playsInline
      />
    </Frame>

    ### Verificar la instalación

    Para verificar la conexión, inicia una nueva sesión de chat en **Antigravity** y envía este mensaje a tu agente:

    ```
    I'm using Yarah as my backend platform, call Yarah MCP's fetch-docs tool to
    learn about Yarah instructions.
    ```

    Si la conexión se realiza correctamente, verás el indicador de estado **MCP Connected** en tu panel de Yarah (esquina superior derecha).

    ![MCP Connected](/images/mcp-setup/mcp-connected.png)
  </Accordion>

  <Accordion title="Kiro">
    ### Instalación

    Agrega el servidor MCP de Yarah a tu **Kiro**:

    1. Selecciona **Kiro** en la lista desplegable
    2. Copia y pega el siguiente comando de instalación en tu terminal de **Kiro**

    ![Kiro Setup](/images/mcp-setup/kiro-1.png)

    3. Ejecuta el comando

    <Frame>
      <video
        src="/images/mcp-setup/Kiro-MCP.mp4"
        autoPlay
        muted
        loop
        playsInline
      />
    </Frame>

    ### Verificar la instalación

    Para verificar la conexión, inicia una nueva sesión de chat en **Kiro** y envía este mensaje a tu agente:

    ```
    I'm using Yarah as my backend platform, call Yarah MCP's fetch-docs tool to
    learn about Yarah instructions.
    ```

    Si la conexión se realiza correctamente, verás el indicador de estado **MCP Connected** en tu panel de Yarah (esquina superior derecha).

    ![MCP Connected](/images/mcp-setup/mcp-connected.png)
  </Accordion>

  <Accordion title="Codex">
    ### Instalación

    Agrega el servidor MCP de Yarah a tu **Codex**:

    1. Selecciona **Codex** en la lista desplegable
    2. Copia y pega el siguiente comando de instalación en tu terminal

    ![Codex Setup](/images/mcp-setup/codex-1.png)

    3. Ejecuta el comando

    <Frame>
      <video
        src="/images/mcp-setup/Codex-MCP.mp4"
        autoPlay
        muted
        loop
        playsInline
      />
    </Frame>

    ### Verificar la instalación

    Para verificar la conexión, inicia una nueva sesión de chat en **Codex** y envía este mensaje a tu agente:

    ```
    I'm using Yarah as my backend platform, call Yarah MCP's fetch-docs tool to
    learn about Yarah instructions.
    ```

    Si la conexión se realiza correctamente, verás el indicador de estado **MCP Connected** en tu panel de Yarah (esquina superior derecha).

    ![MCP Connected](/images/mcp-setup/mcp-connected.png)
  </Accordion>

  <Accordion title="Cline">
    ### Instalación

    Agrega el servidor MCP de Yarah a tu **Cline**:

    1. Selecciona **Cline** en la lista desplegable
    2. Copia y pega el siguiente comando de instalación en tu terminal de **Cline**

    ![Cline Setup](/images/mcp-setup/cline-1.png)

    3. Ejecuta el comando

    ![Cline Install](/images/mcp-setup/cline-2.png)

    ### Verificar la instalación

    Para verificar la conexión, inicia una nueva sesión de chat en **Cline** y envía este mensaje a tu agente:

    ```
    I'm using Yarah as my backend platform, call Yarah MCP's fetch-docs tool to
    learn about Yarah instructions.
    ```

    Si la conexión se realiza correctamente, verás el indicador de estado **MCP Connected** en tu panel de Yarah (esquina superior derecha).

    ![MCP Connected](/images/mcp-setup/mcp-connected.png)
  </Accordion>

  <Accordion title="Windsurf">
    ### Instalación

    Agrega el servidor MCP de Yarah a tu **Windsurf**:

    1. Selecciona **Windsurf** en la lista desplegable
    2. Copia y pega el siguiente comando de instalación en tu terminal de Windsurf

    ![Windsurf Setup](/images/mcp-setup/windsurf-1.png)

    3. Ejecuta el comando

    ### Verificar la instalación

    Para verificar la conexión, inicia una nueva sesión de chat en **Windsurf** y envía este mensaje a tu agente:

    ```
    I'm using Yarah as my backend platform, call Yarah MCP's fetch-docs tool to
    learn about Yarah instructions.
    ```

    Si la conexión se realiza correctamente, verás el indicador de estado **MCP Connected** en tu panel de Yarah (esquina superior derecha).

    ![MCP Connected](/images/mcp-setup/mcp-connected.png)
  </Accordion>

  <Accordion title="Trae">
    ### Instalación

    Agrega el servidor MCP de Yarah a tu **Trae**:

    1. Selecciona **Trae** en la lista desplegable
    2. Copia y pega el siguiente comando de instalación en tu terminal de **Trae**

    ![Trae Setup](/images/mcp-setup/trae-1.png)

    3. Ejecuta el comando

    ![Trae Install](/images/mcp-setup/trae-2.png)

    Alternativamente, Trae ofrece una integración propia con Yarah, que te permite instalar Yarah directamente desde el marketplace oficial de MCP de Trae:

    1. Abre **Settings** de Trae → navega a **MCP**
    2. Haz clic en **Add** y luego selecciona **Add from Marketplace**
    3. Busca "Yarah" y agrégalo desde el marketplace

    ![Trae Marketplace](/images/mcp-setup/trae-3.png)

    4. Para las credenciales requeridas API_KEY y API_BASE_URL, puedes encontrarlas en tu **Settings** de Yarah, en la pestaña **Connect**

    ![Trae Credentials](/images/mcp-setup/trae-4.png)

    ### Verificar la instalación

    Para verificar la conexión, inicia un nuevo chat en **Trae** y envía este mensaje a tu agente:

    ```
    I'm using Yarah as my backend platform, call Yarah MCP's fetch-docs tool to
    learn about Yarah instructions.
    ```

    Si la conexión se realiza correctamente, verás el indicador de estado **MCP Connected** en tu panel de Yarah (esquina superior derecha).

    ![MCP Connected](/images/mcp-setup/mcp-connected.png)
  </Accordion>

  <Accordion title="Qoder">
    ### Instalación

    Agrega el servidor MCP de Yarah a tu **Qoder**:

    1. Selecciona **Qoder** en la lista desplegable
    2. Copia y pega el siguiente comando de instalación en tu terminal de **Qoder**

    ![Qoder Setup](/images/mcp-setup/qoder-1.png)

    3. Ejecuta el comando

    ### Verificar la instalación

    Para verificar la conexión, inicia una nueva sesión de chat en **Qoder** y envía este mensaje a tu agente:

    ```
    I'm using Yarah as my backend platform, call Yarah MCP's fetch-docs tool to
    learn about Yarah instructions.
    ```

    Si la conexión se realiza correctamente, verás el indicador de estado **MCP Connected** en tu panel de Yarah (esquina superior derecha).

    ![MCP Connected](/images/mcp-setup/mcp-connected.png)
  </Accordion>

  <Accordion title="Roo Code">
    ### Instalación

    Agrega el servidor MCP de Yarah a tu **Roo Code**:

    1. Selecciona **Roo Code** en la lista desplegable
    2. Copia y pega el siguiente comando de instalación en tu terminal de **Roo Code**

    ![Roo Code Setup](/images/mcp-setup/roocode-1.png)

    3. Ejecuta el comando

    ### Verificar la instalación

    Para verificar la conexión, inicia una nueva sesión de chat en **Roo Code** y envía este mensaje a tu agente:

    ```
    I'm using Yarah as my backend platform, call Yarah MCP's fetch-docs tool to
    learn about Yarah instructions.
    ```

    Si la conexión se realiza correctamente, verás el indicador de estado **MCP Connected** en tu panel de Yarah (esquina superior derecha).

    ![MCP Connected](/images/mcp-setup/mcp-connected.png)
  </Accordion>

  <Accordion title="OpenCode">
    ### Instalación

    Agrega el servidor MCP de Yarah a tu **OpenCode**:

    1. Selecciona **OpenCode** en la lista desplegable
    2. Copia y pega el siguiente comando de instalación en tu terminal

    ![Connect Project](/images/mcp-setup/connect-project.png)

    3. Ejecuta el comando

    ### Verificar la instalación

    Para verificar la conexión, inicia una nueva sesión de chat en **OpenCode** y envía este mensaje a tu agente:

    ```
    I'm using Yarah as my backend platform, call Yarah MCP's fetch-docs tool to
    learn about Yarah instructions.
    ```

    Si la conexión se realiza correctamente, verás el indicador de estado **MCP Connected** en tu panel de Yarah (esquina superior derecha).

    ![MCP Connected](/images/mcp-setup/mcp-connected.png)
  </Accordion>

  <Accordion title="MCP JSON">
    ### Instalación

    Agrega el servidor MCP de Yarah a tu agente de codificación con MCP JSON:

    1. Selecciona **MCP JSON** en la lista desplegable
    2. Copia y pega la siguiente configuración de **MCP JSON** en tu agente

    ![MCP JSON Setup](/images/mcp-setup/mcp-json-1.png)

    ### Verificar la instalación

    Para verificar la conexión, inicia una nueva sesión de chat y envía este mensaje a tu agente:

    ```
    I'm using Yarah as my backend platform, call Yarah MCP's fetch-docs tool to
    learn about Yarah instructions.
    ```

    Si la conexión se realiza correctamente, verás el indicador de estado **MCP Connected** en tu panel de Yarah (esquina superior derecha).

    ![MCP Connected](/images/mcp-setup/mcp-connected.png)
  </Accordion>
</AccordionGroup>

## MCP remoto

La forma más rápida de empezar es con MCP remoto. Un solo comando intenta la instalación e intentará gestionar la autenticación y la vinculación del proyecto, pero puede que aun así te pida pasos manuales de autenticación o vinculación de proyecto si el flujo automático no puede completarse.

Ejecuta el siguiente comando en tu terminal:

```bash
npx add-mcp https://mcp.yarah.dev/mcp
```

<Note>
  El comando `add-mcp` funciona para la mayoría de los agentes de codificación, pero no para todos. Actualmente admite:
  Claude Code, Claude Desktop, Codex, Cursor, Gemini CLI, Goose, GitHub Copilot, OpenCode, VS Code, Zed.
</Note>

Después de ejecutar el comando, el sistema generará automáticamente un archivo de configuración para tu agente de codificación.

### Conectar y vincular el proyecto

Algunos clientes MCP te pedirán iniciar sesión automáticamente durante la configuración, mientras que otros pueden requerir pasos manuales de autenticación o vinculación de proyecto si el flujo automático no puede completarse. De cualquier forma, la autenticación abrirá una ventana del navegador donde podrás iniciar sesión en tu cuenta de Yarah y conceder acceso de organización/proyecto al cliente MCP.

<AccordionGroup>
  <Accordion title="Cursor">
    Encontrarás el archivo de configuración en `.cursor/mcp.json` como se muestra a continuación:

    ```json
    {
      "mcpServers": {
        "yarah": {
          "url": "https://mcp.yarah.dev/mcp"
        }
      }
    }
    ```

    En el menú **Preferences → Cursor Settings → Tools & MCP**, deberías ver que el servidor MCP de Yarah se agregó correctamente.
    El servidor MCP de Yarah está en el estado "Needs authentication"; puedes hacer clic en el botón `connect` para autenticarte.
  </Accordion>

  <Accordion title="Claude Code">
    Después de la instalación, encontrarás el archivo de configuración en `.mcp.json` como se muestra a continuación:
    ```json
    {
      "mcpServers": {
        "yarah": {
          "type": "http",
          "url": "https://mcp.yarah.dev/mcp"
        }
      }
    }
    ```
    Luego, en una terminal normal (no en la extensión del IDE), ejecuta:
    ```sh
    claude /mcp
    ```
    Selecciona el servidor "yarah" y luego "Authenticate" para iniciar el flujo de autenticación.

  </Accordion>

  <Accordion title="GitHub Copilot">
    Después de la instalación, encontrarás el archivo de configuración en `.vscode/mcp.json` como se muestra a continuación:
    ```json
    {
      "servers": {
        "yarah": {
          "type": "http",
          "url": "https://mcp.yarah.dev/mcp"
        }
      }
    }
    ```
  </Accordion>

  <Accordion title="Google Antigravity">
    Agrega esta configuración a `~/.gemini/antigravity/mcp_config.json`:
    ```json
    {
      "mcpServers": {
        "yarah": {
          "serverUrl": "https://mcp.yarah.dev/mcp"
        }
      }
    }
    ```
    Luego reinicia Antigravity; se autenticará automáticamente.
  </Accordion>

  <Accordion title="Gemini">
    Después de la instalación, encontrarás el archivo de configuración en `.gemini/settings.json` como se muestra a continuación:
    ```json
    {
      "mcpServers": {
        "yarah": {
          "type": "http",
          "url": "https://mcp.yarah.dev/mcp"
        }
      }
    }
    ```
    Inicia la Gemini CLI y ejecuta el siguiente comando para autenticar el servidor:
    ```sh
    /mcp auth yarah
    ```
  </Accordion>

  <Accordion title="Codex">
    <Note>
      Si es la primera vez que usas Codex, es posible que debas habilitar la función rmcp. Para ello, agrega lo siguiente a tu archivo `~/.codex/config.toml`:
      ```toml
      [beta]
      rmcp = true
      ```
    </Note>
    Después de la instalación, encontrarás el archivo de configuración en `.codex/config.toml` como se muestra a continuación:
    ```toml
    [mcp_servers.yarah]
    type = "http"
    url = "https://mcp.yarah.dev/mcp"
    ```
    Luego autentícate:
    ```sh
    codex mcp login yarah
    ```
    Finalmente, ejecuta /mcp dentro de Codex para verificar la autenticación.

    Alternativamente, puedes ejecutar el siguiente comando para instalar el servidor MCP:
    ```sh
    codex mcp add yarah --url https://mcp.yarah.dev/mcp
    ```
  </Accordion>

  <Accordion title="Cline">
    Puedes instalar el servidor MCP de Yarah en tu **Cline** ejecutando el siguiente comando en tu terminal:
    ```bash
    npx -y @smithery/cli mcp add devel/yarah --client cline
    ```
    Alternativamente, puedes seguir la [documentación oficial de Cline](https://docs.cline.bot/mcp/connecting-to-a-remote-server) para agregar el servidor MCP de Yarah con los siguientes parámetros:
    - Server Name: `yarah`
    - Server URL: `https://mcp.yarah.dev/mcp`
    - Transport Type: `Streamable HTTP`

    Después de agregar el servidor, verás un mensaje de error que solicita autenticarte. Haz clic en el botón "Authenticate" que aparece para autenticarte.

  </Accordion>

  <Accordion title="Windsurf">
    Agrega esta configuración a `~/.codeium/windsurf/mcp_config.json`:
    ```json
    {
      "mcpServers": {
        "yarah": {
          "command": "npx",
          "args": [
            "-y",
            "mcp-remote",
            "https://mcp.yarah.dev/mcp"
          ]
        }
      }
    }
    ```
    Windsurf actualmente no admite servidores MCP remotos mediante transporte HTTP. Debes usar el paquete mcp-remote como proxy.
  </Accordion>

  <Accordion title="Roo Code">
    Puedes instalar el servidor MCP de Yarah en tu **Roo Code** ejecutando el siguiente comando en tu terminal:
    ```bash
    npx -y @smithery/cli mcp add devel/yarah --client roocode
    ```
  </Accordion>

  <Accordion title="OpenCode">
    Después de la instalación, encontrarás el archivo de configuración en `./opencode.json` como se muestra a continuación:
    ```json
    {
      "mcp": {
        "yarah": {
          "type": "remote",
          "url": "https://mcp.yarah.dev/mcp",
          "enabled": true
        }
      }
    }
    ```
    Luego ejecuta el siguiente comando para autenticarte:
    ```sh
    opencode mcp auth yarah
    ```
    Esto abrirá tu navegador para completar el flujo de autenticación OAuth.
  </Accordion>
</AccordionGroup>

### Verificar la instalación

Para verificar la conexión, inicia una nueva sesión de chat en tu asistente de codificación con IA y envía este mensaje:

```
I'm using Yarah as my backend platform, call Yarah MCP's fetch-docs tool to
learn about Yarah instructions.
```

Si la conexión se realiza correctamente, verás el indicador de estado **MCP Connected** en tu panel de Yarah (esquina superior derecha).

![MCP Connected](/images/mcp-setup/mcp-connected.png)

## Solución de problemas

<Accordion title="Los cambios no surten efecto">
  La mayoría de los clientes de IA requieren un reinicio completo después de cambios en la configuración de MCP. Cierra y vuelve a abrir la aplicación.
</Accordion>
